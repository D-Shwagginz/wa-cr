require "./wad-reader/**"

# mywad = WAD.read("./rsrc/DOOM.WAD")
# graphic = mywad.graphics.find! { |m| m.name == "STFEVL0" }
# flat = mywad.flats.find! { |m| m.name == "F_SKY1" }
# palette = mywad.playpal.palettes[0]
# test_image = WAD::Graphic.to_tex(graphic, palette)
# test_flat_image = WAD::Flat.to_tex(flat, palette)

# R.export_image?(test_image, "./rsrc/test_image.png")
# R.export_image?(test_flat_image, "./rsrc/test_flat_image.png")

# R.unload_image(test_image)
# R.unload_image(test_flat_image)
